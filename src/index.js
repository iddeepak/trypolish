import ReactDOM from 'react-dom/client';
import './index.css';  // Tailwind directives + global CSS
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);
